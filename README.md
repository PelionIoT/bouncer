# bouncer

Proxy a unix socket

## Build

```
git clone https://github.com/PelionIoT/bouncer.git
cd bouncer
mkdir build
cd build
cmake ..
make
```

## Run

```
./bouncer <listening socket path> <docker.sock path>
```
